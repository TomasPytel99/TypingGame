using System.Windows.Controls;

namespace TypingGame.GameLogic
{
    class Fruit(Canvas canvas, string pathToImage, char letter, int x, int y) : Item(canvas, pathToImage, letter, x, y)
    {
    }
}
